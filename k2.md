# K2 — Kết: VIỆC ĐÃ XONG

Chạy `date +"%Y-%m-%d %H:%M"` = `finished_at`.

## 1. Hoàn thiện log 3 trục

`memory/` `plans/` `reports/` dạng `<STAMP>-[Nội dung].md`, và cập nhật ngay tóm tắt + liên kết vào `README.md` của trục đó (Single Source of Truth).

## 2. Đóng Work Log

`reports/<STAMP>-*.md`:

- §Chat Sessions: đóng dòng cuối
- §Work Log: `status: done`, `outcome`, `duration_min` = **tổng các Chat Sessions** (không phải `finished − started`), `lead_time_h` = `finished − started`

Mẫu hai block: [`blocks.md`](blocks.md)

## 3. README gốc

`README.md` §Đang thực thi: đổi trạng thái hoặc bỏ dòng "Đang làm".

## 4. Push

`git push` nếu cần.

## 5. Báo

Ghi **"Tôi đã xong!"** ở cuối câu chat.
