# K1 — Kết: HẾT CHAT NHƯNG VIỆC CHƯA XONG (checkpoint / bàn giao)

Chạy `date +"%Y-%m-%d %H:%M"` = `ended_at` của cuộc chat này.

## 1. Cập nhật plan

`plans/<STAMP>-*.md`: phần đã xong (✅), phần đang dở, chặn ở đâu.

## 2. Cập nhật report

`reports/<STAMP>-*.md`:

- Đóng dòng hiện tại của §Chat Sessions
- §Work Log: `status: doing` hoặc `paused`
- **`next_actions` phải cụ thể**: đường dẫn file, lệnh cần chạy, ID, host — để phiên sau đọc là làm được ngay, không phải suy luận lại

Mẫu hai block: [`blocks.md`](blocks.md)

## 3. README

`README.md` 3 trục + §Đang thực thi giữ trạng thái `doing` (để M2 tìm lại được).

## 4. Push

`git push` nếu cần.

## 5. Báo

Ghi **"Tôi đã xong!"** ở cuối câu chat — nghĩa là xong **lượt chat này**, không phải xong việc.
