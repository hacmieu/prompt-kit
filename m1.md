# M1 — Mở bài: VIỆC MỚI

Bạn (AI) làm đúng các bước sau **trước khi** bắt tay vào yêu cầu của tôi.

## 1. STAMP

Chạy `date +"%Y%m%d_%H%M"` **ngay**. KHÔNG bịa giờ. Kết quả gọi là `STAMP`, dùng cho **cả việc** (không đổi khi sang cuộc chat khác):

- Tên file log 3 trục: `<STAMP>-[nội-dung].md`
- `work_id = WORK-<STAMP>`
- `started_at` = chính STAMP

## 2. Ngữ cảnh

Đọc `memory/README.md`, `git diff` (nếu có), và các file markdown liên quan tới yêu cầu.

## 3. Scope

Xác định và ghi rõ trong file log:

- `tenant`: `cidare` | `aiha` (kèm `org_unit`: `cpc`|`att`|`heza`|`hth`|`asia`|…)
- `workstream`: `ops`|`backup`|`dns`|`dev`|`admin`

## 4. Neo trạng thái (chống tràn context)

Ngay sau khi nắm ngữ cảnh, **tạo trước** 3 file log `<STAMP>-*.md` trong `memory/` `plans/` `reports/` với `status: doing`, cập nhật `README.md` 3 trục + dòng "Đang làm" ở `README.md` gốc — **dù chưa làm gì**.

Lý do: nếu cuộc chat chết vì tràn context, trạng thái đã nằm trên đĩa và phiên sau đọc được.

## 5. Rồi mới làm

Thực hiện yêu cầu tôi ghi sau dòng snippet này.

---

Hết chat mà chưa xong → dùng [`k1.md`](k1.md). Xong việc → [`k2.md`](k2.md). Làm tiếp ở chat sau → [`m2.md`](m2.md).
