# M2 — Mở bài: TIẾP VIỆC CŨ (kể cả sau khi tràn context)

Bạn (AI) làm đúng các bước sau trước khi làm tiếp.

## 1. Tìm lại việc — KHÔNG tạo STAMP mới

Đọc `README.md` §Đang thực thi để lấy `<STAMP>` và `work_id` của việc đang `doing`.
Không tìm thấy → `ls -t plans/ | head` rồi hỏi tôi xác nhận. **Tuyệt đối không** tạo STAMP mới cho việc đang dở.

## 2. Đọc lại trạng thái

- `plans/<STAMP>-*.md` — kế hoạch + phần còn lại
- `reports/<STAMP>-*.md` — §Chat Sessions, §Work Log, `next_actions`
- `memory/<STAMP>-*.md`
- `git diff` và `git log --oneline -5`

## 3. Ghi phiên chat mới

Chạy `date +"%Y-%m-%d %H:%M"` → thêm một dòng vào §Chat Sessions của `reports/<STAMP>-*.md`.
Nếu phiên trước không có `ended_at` (chết vì tràn context) → điền `~`, không bịa giờ.

## 4. Làm tiếp

Bắt đầu từ `next_actions` gần nhất. **Không** làm lại phần đã đánh dấu xong.

---

Hết chat mà chưa xong → [`k1.md`](k1.md). Xong việc → [`k2.md`](k2.md).
